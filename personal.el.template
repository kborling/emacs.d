;;; personal.el --- Personal settings not tracked in git -*- lexical-binding: t; -*-

;;; Commentary:
;; Copy this file to personal.el and customize with your personal information.
;; The personal.el file is loaded automatically but ignored by git.
;; This keeps your personal information private while sharing your config.

;;; Code:

;; User information
(setq user-full-name "Your Name"
      user-mail-address "your.email@example.com")

;; gptel (Claude API)
;; (setq gptel-api-key "sk-ant-...")

;; Claude session archive — Desktop export directory
;; (setq kdb-claude-export-directory "~/Downloads/")

;; SQL connections (Azure)
;; (setq kdb-sql-connections
;;       '(("prod" :server "prod.database.windows.net" :database "mydb")
;;         ("dev"  :server "dev.database.windows.net"  :database "mydb-dev")))

;; Evil mode — enable per-project or per-buffer
;; (setq kdb-evil-project-list '("/path/to/vim-project"))
;; (setq kdb-evil-buffer-list '("COMMIT_EDITMSG"))

;; Location for redshift night light (EXWM)
;; Format: "latitude:longitude"
;; (setq kdb-location-coordinates "39.9:-82.9")

;; Machine-specific settings
;; (when (string= (system-name) "work-laptop")
;;   (setq some-work-specific-variable t))

(provide 'personal)
;;; personal.el ends here
